#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    cin >> t;
    while (t--) {
        int p, q;
        cin >> p >> q;
        
        int res = 0;

        

        cout << res << endl;
    }
    return 0;
}
